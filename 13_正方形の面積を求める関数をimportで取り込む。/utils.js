// 通常の関数
// function square(length){
//     return length * length;
// };

//アロー関数で記載する場合
export const square = (length) => {
    return length * length;
};

//export defaultを使う場合は、constなどの型は記載しない。
// export default square();
